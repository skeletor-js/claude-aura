import type { ColorsConfig } from "../types.js";

export const DEFAULT_COLORS: ColorsConfig = {
  idle: "#E8DCC8",
  thinking: "#DA7756",
  needs_input: "#E3A869",
};

export const DEFAULT_BRIGHTNESS = 80;
export const DEFAULT_TRANSITION_MS = 1500;
export const DEFAULT_PORT = 7685;
